<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
  <!-- firebase-analytics's own manifest declares this permission by default (for ad-attribution
       features this app doesn't use: AnalyticsHelper only logs internal feature-usage events,
       never anything ad-related). Without this, the merged manifest ends up requesting the
       advertising ID even though nothing in this app's code ever asks for it, which then
       conflicts with the Play Console "does this app use advertising ID?" declaration (answered
       "no", and should stay "no") and blocks release uploads until they match. -->
  <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
  <application android:allowBackup="true" android:icon="@mipmap/ic_launcher_font_creator" android:roundIcon="@mipmap/ic_launcher_font_creator" android:label="@string/app_name" android:theme="@style/Theme.RemoteConfig">
    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.files"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
    </provider>
    <activity android:name=".MainActivity" android:exported="true" android:grantUriPermissions="true" android:launchMode="singleTop">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <action android:name="android.intent.action.SEND_MULTIPLE" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="application/pdf" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.SEND" />
        <action android:name="android.intent.action.SEND_MULTIPLE" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="image/*" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.EDIT" />
        <action android:name="com.google.android.apps.photos.OEM_EDIT" />
        <category android:name="android.intent.category.DEFAULT" />
        <data android:mimeType="image/*" />
      </intent-filter>
    </activity>
  </application>
</manifest>
